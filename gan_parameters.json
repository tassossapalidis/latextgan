{   
    "training_parameters": {
        "epochs": 23,
        "batch_size": 64,
        "n_generator_train": 10,
        "num_train_examples": 200000,
        "learning_rate": 0.0001,
        "weight_decay": 0.00025,
        "beta_1": 0.5,
        "beta_2": 0.9,
        "optimizer": "tf.keras.optimizers.Adam(learning_rate = learning_rate, beta_1 = beta_1, beta_2 = beta_2)"
    },
    "architecture_parameters": {
        "generator_num_layers": 60,
        "discriminator_num_layers": 60
    },
    "model_save_parameters": {
        "checkpoint_directory": "./gan_checkpoints",
        "losses_filename": "./losses.csv",
        "save_frequency": 1,
        "max_to_keep": 3,
        "restore_model": true
    }
}
